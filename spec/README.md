# Specifications
The planner skill writes specifications here before any implementation
(governance principle 3.1: specification before implementation).
Each spec covers: WHAT, WHY, CONSTRAINTS, RISKS, SUCCESS/ACCEPTANCE CRITERIA.

First spec: copy experimental-pipeline-definition.md into this folder —
it already covers the pipeline's what/why/constraints/success criteria and
is the specification the Developer agent implements against.
