# Agent Journal
Evidence base for Appendix A.4 (reflective account). One entry per significant
agent task, appended by the reflection skill or by hand. Keep entries short and dated.

Entry format:
- **Date / task:**
- **What happened:**
- **Where the agent was uncertain or stuck:**
- **Assumptions made:**
- **How I verified the output:**
- **What was learned / should change next time:**

---

- **Date / task:** 2026-08-11 / Implement `spec/synthetic-recovery-validation.md` (T1–T6): rebuild the ground-truth schema, generator, and recovery validator from scratch.
- **What happened:** Wrote `spec/ground-truth-schema.md` (T1); rebuilt `src/generation/generate_datasets.py` (T2/T3) writing `data/raw/generated_<name>.csv` and `configs/ground_truth.json` with `stochastic_form`, `annual_phase_reference_day`, `seasonal_planted` fields and `generated_`-prefixed dataset keys; rebuilt `src/recover_structure.py` (T4) to read those fields explicitly instead of key-presence sniffing / hardcoded phase, and added an explicit per-dataset `TOLERANCES` dict with PASS/FAIL reporting (T5). Ran generation + validation end to end (T6): both datasets pass their tolerance checks; confirmed re-running generation reproduces byte-identical CSVs and ground truth.
- **Where the agent was uncertain or stuck:** Two judgment calls not fully pinned down by the spec: (1) whether `main()`'s exit code should reflect tolerance pass/fail — added `return 1` on any FAIL so the stage can gate a pipeline the way `data_quality.py` does, since the spec's T6 wording ("record ... as a documented exception") didn't specify exit-code behavior; (2) the ED dataset's `TOLERANCES` entry deliberately omits an `aic_prefers_planted_form` check, because AIC preferring ARMA(1,1) over the planted AR(1) is an expected artifact of aggregation noise for that dataset, not a recovery failure — flagging this to the student rather than silently deciding it's fine.
- **Assumptions made:** Reused the previously-reviewed DGP parameter values (base levels, effect sizes, phi/theta/sigma, dropout/volume models) rather than redesigning them, since those numbers were already inspected in an earlier code review and the spec's T2 only asked that they validate against the T1 schema, not that they change. Set tolerances by domain reasoning (tight for job-shop's low-noise ARMA per its "faithful recovery expected" note; looser for ED's weekend/annual effects, no upper bound on AR(1) attenuation since it's expected) — not derived from a formal power calculation.
- **How I verified the output:** Ran the generator and validator together (same-call, since each Bash invocation appears to reset filesystem writes between calls); confirmed exit code 0 and both datasets' `tolerance_pass: true`; separately confirmed byte-identical regeneration of both CSVs and the ground-truth JSON by diffing against a pre-run copy.
- **What was learned / should change next time:** The per-dataset-seed-derivation risk noted in the spec (global seed means inserting a new dataset before existing ones would perturb their random draws) is still unresolved — deferred per "flag, don't silently fix," since the spec listed it as an open trade-off requiring student confirmation, not a decided requirement.

- **Date / task:** 2026-08-11 / Implement `spec/stage0-dataset-screening.md` (T1–T6): rebuild the column inspector and quality profiler from scratch, plus their rule/threshold spec docs.
- **What happened:** Wrote `spec/column-inspection-rules.md` (T1/T2) and `spec/quality-gate-thresholds.md` (T4) — precise formulas for the ambiguity check, the unit-consistency ratio/bands, and all four profiler gates. Rebuilt `src/inspect_columns.py` (T3) and `src/data_quality.py` (T5) against those docs — same behavior as before, with named constants instead of inline magic numbers, plus a genuinely under-specified unit-consistency formula (median-ratio + band classification) replacing the original's ad hoc/hard-to-verify calculation. Built five fixture datasets (T6: one per failing gate + one all-pass) and ambiguous/unambiguous/unit-mismatch inspector fixtures in the session scratchpad (not committed to the repo — throwaway verification aids), ran both tools against all of them: 14/14 acceptance checks passed.
- **Where the agent was uncertain or stuck:** T6 surfaced a real bug during verification, not a design ambiguity: pandas 3.0 (installed in `.venv`) infers CSV string columns as dtype `"str"`, not the legacy `object` dtype the original `s.dtype == object` check assumed — so the "looks like a timestamp" overview flag silently never fired for any string column under this pandas version. Fixed to check `is_object_dtype(s) or is_string_dtype(s)`, which covers both. This was necessary to meet T3's own acceptance criterion (T1a), not scope creep.
- **Assumptions made:** Kept `data_quality.py`'s logic byte-for-byte equivalent to the previously-reviewed version (only added the threshold-doc cross-reference comment); did not add `plt.close(fig)` even though it's arguably good hygiene, because it wasn't asked for by the spec and each profiler run is a separate process per the spec's CONSTRAINTS (so it has no effect there) — flagged rather than silently added, then left out.
- **How I verified the output:** Built and ran a fixture harness (5 profiler datasets engineered to fail-one-gate-each / pass-all, 4 inspector scenarios for overview/ambiguity/unit-consistency) in the session scratchpad; asserted exit codes, named failing checks, and warning/flag presence against the spec's SUCCESS CRITERIA. All 14 assertions pass. Confirmed no fixture files or reports leaked into the repo's `reports/` directory.
- **What was learned / should change next time:** Verification fixtures are worth building even for "just rebuild what was there" tasks — the pandas-version dtype bug would not have been caught by a code read alone, only by actually running the acceptance criteria.

- **Date / task:** 2026-08-12 / Move `ground_truth.json` from `configs/` to repo root.
- **What happened:** Updated every reference (`src/generation/generate_datasets.py`'s write path/docstring/print, `src/recover_structure.py`'s default arg/docstring/usage line, `spec/synthetic-recovery-validation.md`'s design-decisions table and task list, `spec/ground-truth-schema.md`'s title/version note) from `configs/ground_truth.json` to `ground_truth.json`. Regenerated the file at the new location and re-ran the recovery validator end to end to confirm the new path works; both datasets still pass tolerance.
- **Where the agent was uncertain or stuck:** The destination path wasn't specified in the request ("update the path... in all the files it appears" didn't say to what) — asked the student rather than guessing; they chose repo root over `data/` or `data/raw/`.
- **Assumptions made:** Left the historical journal entries describing the earlier `configs/ground_truth.json` work unedited — they're an accurate record of what was true when written; only new entries document the rename.
- **How I verified the output:** `grep -rn "configs/ground_truth"` across the repo (excluding `.venv`/`.git`) returns only the untouched historical journal entry; ran generator + validator together and confirmed exit code 0 with both datasets passing.
- **What was learned / should change next time:** n/a.

- **Date / task:** 2026-08-12 / Remove fabricated "v2 finding, Section 3.7" citation from `generate_datasets.py` and `recover_structure.py`, per updated CLAUDE.md rule 5/6.
- **What happened:** Investigated the ED dataset's AR(1) φ=0.60→0.151 attenuation (larger than CLAUDE.md's earlier-documented "~0.50") per rule 5's instruction to check provenance, σ values, and the aggregation step before accepting a new number. Confirmed regeneration is byte-identical (not a staleness bug) and empirically isolated the cause by simulation: record-level lognormal noise (`lognormal_sigma=0.35`) plus the acuity-factor mixture, at ~83 records/day, dominates the AR(1) innovation noise (σ=5) in the daily-median aggregation — a real DGP-parameter effect, not a code bug. Along the way found the code's "(v2 finding, Section 3.7)" note in both files (and mirrored into `ground_truth.json`/`reports/recovery_validation.txt`) doesn't correspond to anything in the repo; the student confirmed it should be removed. Replaced it with an accurate description of the measured mechanism in both source files, then regenerated `ground_truth.json` and `reports/recovery_validation.txt` from the fixed code rather than hand-editing them.
- **Where the agent was uncertain or stuck:** Whether the citation referred to real material outside the repo (a dissertation draft section) — asked rather than assuming it was fabricated Claude output; student confirmed removal.
- **Assumptions made:** None beyond what was confirmed — no DGP parameters were changed (student's CLAUDE.md edit clarified attenuation is judged only against `recover_structure.py`'s TOLERANCES, which already pass), only the unsupported citation text.
- **How I verified the output:** Regenerated both datasets and reran the validator; `grep -rn "v2 finding\|Section 3.7"` across `src/`, `spec/`, `ground_truth.json`, `reports/`, `journal/` returns nothing.
- **What was learned / should change next time:** When a rule references a specific expected number (e.g. "~0.50"), treat it as a claim to verify against the current pipeline before trusting it, not just as background context — it can go stale as the DGP evolves.

- **Date / task:** 2026-08-12 / Implement `spec/cross-domain-eda.md` + `spec/eda-parameters.md` (T1–T6): new EDA stage, `src/eda.py`.
- **What happened:** Planner drafted `spec/cross-domain-eda.md`, then revised twice per student feedback: (1) EDA must run only against datasets currently passing `data_quality.py`'s gate (checked live, not assumed from the "final four" label), and (2) all EDA output must live under `reports/eda/`, separate from Stage-0 quality reports and `recovery_validation.txt`. Wrote `spec/eda-parameters.md` (T1: per-dataset unit-conversion factors, STL period, seasonal-boxplot grouping, ADF α, seasonality-strength threshold). Implemented `src/eda.py` (T2–T4): a gate-check helper that re-runs `data_quality.py`'s own `profile()` (not a re-implementation, avoiding a second quality screen) to decide pass/skip; per-dataset basic EDA (descriptive stats at record- and series-level, histogram, time series + rolling mean, seasonal boxplot) and diagnostic EDA (ADF at d=0/d=1, ACF/PACF, STL decomposition with a seasonality-strength score); a cross-domain summary table. Ran it against all four final datasets (T5/T6): all four currently pass the gate and produced reports+plots under `reports/eda/`.
- **Where the agent was uncertain or stuck:** None requiring a stop — the spec was concrete enough (parameters pinned down in T1) that no implementation judgment calls were left open.
- **Assumptions made:** STL's input is linearly interpolated to handle any residual gaps (STL requires no NaNs), but ADF/ACF/PACF/descriptive stats use the un-interpolated series — stated explicitly in both the spec and each report so the two don't get conflated. Series-construction logic (load/resample/aggregate) is duplicated from `data_quality.py` rather than imported, matching `recover_structure.py`'s existing precedent for the same reason (decoupling from an unrelated gating script's import side effects).
- **How I verified the output:** Ran `src/eda.py` twice back-to-back and diffed the cross-domain summary — byte-identical (reproducible, no stochastic component). Cross-checked (T5) the two ground-truth datasets' raw-series ADF p-values against `reports/recovery_validation.txt`'s independently-computed ADF p-values — exact match (0.0826 for ED, 0.0000 for job-shop), confirming the two diagnostic paths agree. Verified the skip path by running against a config known to fail the gate (`call_center_operations_wfo_dataset.yaml`, per `spec/call_center_data_prep.md`'s documented FAIL) — correctly skipped with no EDA output, reported in the summary's "SKIPPED" section.
- **What was learned / should change next time:** n/a.

- **Date / task:** 2026-08-12 / Fix STL-plot unit bug in `src/eda.py`; write one EDA interpretation doc per dataset.
- **What happened:** While reading each dataset's plots to write a plain-language interpretation, found `run_eda()`'s STL step used `series` (native units — seconds for call-center, hours for IT-support) instead of `series_minutes`, violating `cross-domain-eda.md`'s "convert to minutes for all plots" decision. Fixed the one-line bug and reran `src/eda.py`; confirmed `seasonal_strength`/`visible_seasonality` in the text reports and cross-domain summary were unaffected (it's a variance ratio, scale-invariant), only the STL plot's y-axis units changed. Then viewed all 20 plots (5 per dataset × 4) and wrote `reports/eda/<name>_eda_interpretation.md` for each — plain-language explanation of every number and every graph, plus what it implies for the upcoming ARIMA-fitting stage.
- **Where the agent was uncertain or stuck:** None requiring a stop.
- **Assumptions made:** For `generated_emergency_dept_stays`, explicitly distinguished the raw-series PACF lag-1 value (0.504, mixes deterministic weekend/annual pattern with latent AR(1) persistence) from `recover_structure.py`'s residual-based recovered φ (0.151) — these answer different questions and conflating them would overstate the raw autocorrelation's AR(1) content; flagged rather than picked one number to report. For `call_center_emea_2021_2025`, noted the STL seasonality classification (F_s=0.331, just over the 0.3 threshold → "y") sits in tension with the visually flat monthly boxplot — reported both rather than letting the threshold silently win.
- **How I verified the output:** Regenerated all EDA output post-fix and reread the corrected STL plots directly (all four now show minute-scale axes). Every number quoted in the four interpretation docs was taken directly from the regenerated `_eda.txt` reports, not recalled from memory.
- **What was learned / should change next time:** Read the actual rendered plots before writing about them, not just the text report — the STL unit bug wasn't visible in any number in the text report (F_s is scale-invariant) and would only have been caught by looking at the image itself.

- **Date / task:** 2026-08-13 / Implement `spec/baseline-forecasting-models.md` + `spec/baseline-parameters.md` (T1–T6): `src/baselines.py`.
- **What happened:** Wrote `spec/baseline-parameters.md` (T1: MA window k=7, SES via `statsmodels.SimpleExpSmoothing(train, initialization_method="estimated").fit()`, train/test split reusing `data_quality.py`'s `TRAIN_FRACTION=0.8`, MAPE zero-guard at `|actual|<=1e-9`, output paths). Implemented `src/baselines.py` (T2–T5): same quality-gate-import + series-construction-duplication pattern as `eda.py`; four walk-forward one-step-ahead baselines (naive, historical mean, moving average, SES — SES fits α once on train via `SimpleExpSmoothing`, then continues the smoothing recursion using revealed test actuals without re-fitting α); MAE/RMSE/MAPE plus per-test-point forecast/error persistence to CSV; per-dataset report + cross-domain summary under `reports/baselines/`.
- **Where the agent was uncertain or stuck:** None requiring a stop — spec's design-decisions table left no open implementation calls.
- **Assumptions made:** Train/test split uses positional slicing (first n_train / last n_test rows) rather than `data_quality.py`'s notna()-count-based n_train — documented in `baseline-parameters.md` as equivalent only because all four final datasets currently have 0% missing periods; would need revisiting if a future qualifying dataset had gaps. Error sign convention fixed as `actual - forecast` (residual convention), stated explicitly in every report rather than left implicit.
- **How I verified the output:** T3/T4: ran all four baseline formulas plus the MAPE zero-guard against a hand-built series (`1..10`) in a scratch check and confirmed every forecast matched manual arithmetic exactly, including the SES recursion re-derived by hand from the fitted α. T6: ran the full pipeline against all four datasets twice and diffed both the cross-domain summary and all four per-point forecast CSVs — byte-for-byte identical, so the SES-determinism risk flagged in the spec did not materialize (confirmed, not assumed). Sanity-checked the fitted α values against EDA's own diagnostics: `synthetic_it_support_tickets` (near-zero ACF/PACF per its EDA interpretation) converged to α≈0 (SES ≈ constant level, closely tracking the historical-mean baseline's MAE); `generated_job_shop_manufacturing` (ACF lag-1=0.90 per its EDA interpretation) converged to α=1.0 (SES degenerates to naive, and its metrics are exactly equal to naive's) — both are the expected behavior for those series' known autocorrelation structure, not anomalies.
- **What was learned / should change next time:** n/a.

- **Date / task:** 2026-08-14 / Add plots to `src/baselines.py` (student request, not in original spec).
- **What happened:** Amended `spec/baseline-forecasting-models.md` (WHAT item 6, a new design-decisions row) before implementing, since the original spec never included visualizations at all — a real gap, not something intentionally scoped out, surfaced only when asked to add plots. Added two: a per-dataset actual-vs-forecast overlay (`<name>_forecasts_plot.png`, native duration unit) and a cross-domain comparison chart (`baselines_mape_comparison.png`). While designing the cross-domain chart, found `build_series()`'s duration values are each dataset's *native* unit (hours/seconds/minutes, never normalized the way `eda.py` converts everything to minutes) — a pre-existing gap in `baselines.py`, not something introduced by this change. Used MAPE (already unit-independent) for the cross-domain chart instead of MAE/RMSE to sidestep the mixed-unit problem, and documented the underlying gap in the spec rather than fixing it silently, since fixing it would change every already-reported MAE/RMSE number.
- **Where the agent was uncertain or stuck:** None requiring a stop — flagged the unit gap to the student in conversation before implementing, rather than silently picking MAPE without explanation.
- **Assumptions made:** None beyond the MAPE choice above.
- **How I verified the output:** Viewed all five new plots directly (four per-dataset overlays + the cross-domain bar chart) — visually consistent with the numeric results (e.g. `generated_job_shop_manufacturing`'s naive line is fully hidden behind SES's, matching their identical MAE/RMSE/MAPE from α=1.0). Re-diffed the cross-domain summary text and forecast CSVs against the pre-plot run — unchanged, confirming the plotting addition didn't alter any numeric logic.
- **What was learned / should change next time:** When a spec is approved and later extended piecemeal (plots added after the fact), re-check for gaps the original spec's own scope decisions didn't anticipate (unit normalization, in this case) rather than only checking the new request in isolation.

- **Date / task:** 2026-08-14 / Implement `spec/arima-fitting.md` + `spec/arima-parameters.md` (T1–T9): `src/arima_model.py`, the main ARIMA-fitting/tuning stage.
- **What happened:** Before drafting the spec, asked the student to lock three open methodological calls rather than assume them: plain ARIMA for all four datasets (no SARIMA, even on the two EDA-confirmed-seasonal ones), manual Box-Jenkins order selection (ADF fixes `d`, ACF/PACF bounds candidate `p`/`q`, AIC selects among candidates — matching `CLAUDE.md`'s named diagnostic sequence exactly, no `pmdarima`), and fit-once-then-extend walk-forward forecasting (mirrors the baseline stage's SES precedent: order+coefficients frozen after the training fit, extended via `append(refit=False)`, never refit). Wrote both spec docs, then implemented `src/arima_model.py` following the same quality-gate-import + series-construction-duplication pattern as `eda.py`/`baselines.py`. Ran end-to-end against all four final datasets (T9): `generated_job_shop_manufacturing` selected ARIMA(1,0,1) — matching its ground-truth ARMA(1,1) exactly, consistent with `recover_structure.py`'s independently-computed AIC-preferred order; `call_center_emea_2021_2025` selected ARIMA(0,0,0) (near-noise, as its EDA/baseline reports already suggested); `generated_emergency_dept_stays` selected ARIMA(5,1,1) and `synthetic_it_support_tickets` selected ARIMA(5,0,2), both hitting the `P_MAX=Q_MAX=5` ceiling.
- **Where the agent was uncertain or stuck:** Hit a real bug during T6 verification, not a design ambiguity: `ARIMAResults.append(new_obs, refit=False)` raised `ValueError: Columns must match to concatenate along rows` because `train`'s series has `.name == "duration"` (from `build_series()`'s `df.set_index(ts_col)["duration"]`) while the per-step `pd.Series([actual], index=[ts])` I first wrote had `name=None` — statsmodels' internal `append` concatenates on column name, not position. Fixed by passing `name=train.name` explicitly. This wasn't caught by the earlier synthetic-series hand check (T6) because that test series had no name either (both sides were `None`, so they matched) — a reminder that hand-check fixtures need to mirror the real data's exact shape (named vs. unnamed Series), not just its statistical properties.
- **Assumptions made:** Manually re-derived the AR(1) one-step forecast formula to verify walk-forward forecasting (T6) and got it wrong on the first attempt — assumed statsmodels' `const` parameter was the AR-equation intercept (`mean = const/(1-phi)`) when for `ARIMA(..., trend='c')` it's actually the process mean directly (`mean = const`). Caught by the assertion failing, not assumed correct; recomputed with the right formula and reconfirmed the match, including the `append(refit=False)` state-extension step.
- **How I verified the output:** T3/T4/T6: hand-built a synthetic AR(1) series (known φ=0.8) and asserted `select_d` resolves `d=0`, `acf_pacf_bounds` finds lag 1 significant in PACF (matching AR(1)'s theoretical sharp cutoff), and the first two walk-forward forecasts match the manually-derived AR(1) one-step formula exactly (to 1e-6). T9: ran the full pipeline twice and diffed all of `reports/arima/` (byte-for-byte identical — MLE-optimizer non-determinism, flagged as a risk in the spec, did not materialize on this platform/run). Cross-checked `generated_job_shop_manufacturing`'s selected order against `recover_structure.py`'s already-passing recovery validation (both independently arrive at AIC-preferring ARMA(1,1)/(1,0,1)) — two separately-computed diagnostics agreeing on the same dataset. Viewed all four forecast-overlay plots directly: `generated_job_shop_manufacturing`'s ARIMA line tracks tightly (expected — low-noise, faithful-recovery dataset); `generated_emergency_dept_stays`'s ARIMA line visibly smooths through the weekly spikes (expected and reportable per the spec's locked "plain ARIMA on seasonal data will underperform" risk, confirmed visually rather than assumed from the metrics alone).
- **What was learned / should change next time:** When hand-building synthetic verification fixtures for a Series-based API, match incidental structure (an explicit `.name`) as well as statistical properties — an unnamed synthetic series can pass a check that a real, named series would fail, exactly what happened here with `append()`'s column-matching behavior.

- **Date / task:** 2026-08-15 / Implement `spec/model-evaluation.md` + `spec/model-evaluation-parameters.md` (T1–T7): `src/evaluate_model.py`, comparing ARIMA against the four baselines with significance testing.
- **What happened:** This stage went through an unusual amount of back-and-forth before implementation, all recorded in the spec itself rather than silently decided: originally specced around a Wilcoxon signed-rank test (since `CLAUDE.md` flags Diebold–Mariano as unconfirmed by the supervisor); ran a diagnostic (`check_dt_autocorrelation.py`, scratch, Ljung-Box on all 16 dataset/baseline loss-differential series) which found 5 pairs with real serial autocorrelation — exactly what Wilcoxon can't correct for; on the student's explicit instruction, revised both spec files to use DM instead (implemented as OLS-on-a-constant with HAC/Newey–West covariance, `statsmodels` only, no new dependency), explicitly marked "provisional pending supervisor confirmation" in every line of output, and drafted a short message for the student to send their supervisor. `CLAUDE.md`'s own "not confirmed" line was deliberately left unedited — confirmation hasn't arrived yet. Implemented `src/evaluate_model.py` reading only the already-persisted `reports/arima/*_arima_forecasts.csv` and `reports/baselines/*_baselines_forecasts.csv` (no refitting), Ljung-Box + DM per pair, Holm–Bonferroni within each dataset's 4-test family, win/loss/tie classification, per-dataset reports, cross-domain summary, and a win/loss heatmap (included proactively this time rather than as a follow-up request, since that pattern had repeated twice already).
- **Where the agent was uncertain or stuck:** The spec's T4 acceptance criterion said a fixture with "identical ARIMA/baseline errors" should classify as `no_significant_difference` — but a literal all-zero loss differential instead hits the separate `no_test_possible` guard (DM is degenerate when `d_t` is uniformly zero). Resolved by interpreting "identical" as "no systematic difference" (same-distribution noisy errors, not bit-identical) for that fixture, and building a third, explicitly bit-identical fixture to confirm the `no_test_possible` path separately — flagged here as a spec-wording ambiguity resolved by interpretation, not silently guessed past.
- **Assumptions made:** None beyond the fixture-wording interpretation above.
- **How I verified the output:** T3: cross-checked every one of the 16 Ljung-Box results against the earlier scratch diagnostic's numbers — exact match across all four datasets, confirming the "production" implementation reproduces the exploratory one. T3/T4: three hand-built fixtures (ARIMA uniformly better by a fixed margin → `arima_wins` with p≈0; same-distribution noise on both sides → `no_significant_difference`; literally identical errors → `no_test_possible`) all matched expectations. Caught one real bug this way indirectly: the job-shop naïve/SES "identical baseline" note (required by the spec, and by `arima-fitting`'s earlier-documented SES α≈1.0 finding) wasn't firing — `np.allclose(..., atol=1e-9)` was too tight, because SES's α is fit by numerical optimization and lands at ~0.9999999, not exactly 1.0, so the two baselines' errors differ by ~1e-7 despite being identical at the 4-decimal precision everything is reported at. Loosened to `atol=1e-4` (matching reporting precision) and confirmed the note now fires and both baselines' DM stats are numerically identical as expected. T7: ran end-to-end twice and diffed all of `reports/evaluation/` — byte-identical (no stochastic fitting in this stage, so exact reproducibility was expected, not just hoped for).
- **What was learned / should change next time:** A tolerance chosen for a different purpose (guarding against literal float noise) is often wrong for a *reporting* judgment call ("are these two things practically the same") — the right tolerance there should match the precision the reader actually sees (4-decimal-place metrics), not an arbitrarily tight float-equality bound. Also: when a diagnostic script written earlier in the same conversation produces numbers that a later "real" implementation should reproduce, diffing against it directly (not just re-eyeballing) is a cheap, strong correctness check — worth doing whenever that opportunity exists.

- **Date / task:** 2026-08-16 / Implement `spec/results-analysis.md` + `spec/results-analysis-parameters.md` (T1–T5): `src/analyse_results.py` and `reports/analysis/rq1_rq2_analysis.md`, the final "analyse and interpret results" pipeline stage.
- **What happened:** Wrote a small join script (`src/analyse_results.py`) that parses only the trailing `json.dumps(row)` lines each of the four prior stages' cross-domain summaries already write, merges them on `dataset` (inner join — a dataset needs to appear in all four to get a row), and fails loudly (raises, names the source file and missing key) rather than producing a blank if a source's schema ever drifts. Ran it end-to-end: all four final datasets joined cleanly with no exclusions. Then wrote the actual deliverable — `reports/analysis/rq1_rq2_analysis.md` — with the five sections the spec locked (status banner, RQ1 verdict, RQ2 cross-domain variation, aviation-MRO implications, limitations). The RQ2 analysis surfaced a real finding worth flagging to the student even though it wasn't explicitly asked for: seasonality strength (EDA's F_s) does *not* predict where ARIMA wins — `generated_emergency_dept_stays` has the highest F_s (0.522) of all four datasets and yet ARIMA sweeps all four baselines there, while `call_center_emea_2021_2025` has only middling F_s (0.331) but is where ARIMA underperforms most. What actually lines up with the win/tie pattern is whether AIC/BIC order selection found confident, unambiguous structure (job-shop and ED both did; call-center and IT-support-tickets both didn't) — a more interesting and defensible RQ2 story than "seasonality causes underperformance," and one the master table made easy to check by putting F_s, order-selection confidence, and MAPE side by side.
- **Where the agent was uncertain or stuck:** None requiring a stop — the spec's fixed five-section structure and "trace every claim to a source" rule left little open to interpret.
- **Assumptions made:** None beyond what the spec already locked (JSON-lines-only sourcing, no new computation, aviation-MRO section framed as analogy/inference given no MRO dataset was tested).
- **How I verified the output:** T2: deliberately corrupted a scratch copy of `eda_cross_domain_summary.txt` (dropped one JSON key) and confirmed the loader raises `KeyError` naming the file and the missing key, rather than silently producing a blank column; restored the real file and reran to confirm it's back to a clean 4-row join. T3: ran the script twice and diffed `reports/analysis/` — byte-identical (purely a file read/join, no stochastic step). T5: spot-checked every numeric claim in the narrative document against its cited source — RQ1 table's Holm-adjusted p-values against each `reports/evaluation/<name>_evaluation.txt` (and cross-checked against the win/loss heatmap's own rounding, since both round to 3 decimals), RQ2 table's seasonal-strength/order/MAPE values against `master_evidence_table.csv`, and the "no cell is a baseline win" claim against the master table's `baseline_wins` column (0 for all four rows) — all matched exactly.
- **What was learned / should change next time:** Putting independently-computed diagnostics side by side in one table (here: EDA's seasonality measure next to ARIMA's own order-selection confidence next to the DM win/tie outcome) is what surfaced the "it's not seasonality, it's order-selection confidence" finding — a conclusion that wasn't visible from any single prior stage's report in isolation. Building the reproducible join table before writing the narrative (rather than writing the narrative first and citing numbers from memory) made this cheap to notice and made every subsequent claim in the document mechanically spot-checkable.
